
# 欢迎来到 python-tiny-claw 项目工作区
## 架构说明
- 本项目采用 Python 语言编写，追求极致性能。
- 所有的 API 接口都必须返回 JSON 格式，且包含 `code` 和 `message` 字段。
- 所有的错误处理，必须返回中文报错信息，绝对禁止使用英文抛错。

## 注意事项
- bash下应使用python3执行python脚本。
  
## 禁忌事项
- 不允许删除根目录的任何文件。
- 不许在没有看任何项目代码的情况下做出任何判断。
  